import React from 'react';

const Constraints = ({ constraints }) => {
    return (
       <div>
            <p className="mb-4">{constraints}</p>
        </div>
    );
};

export default Constraints;
